apiVersion: v1
kind: Secret
metadata:
  name: user-management-secrets
  namespace: visionboard
  labels:
    app: user-management-service
type: Opaque
stringData:
  # Database Credentials
  db.username: "postgres"
  db.password: "CHANGE_ME_IN_PRODUCTION"
  
  # Keycloak Credentials
  keycloak.clientSecret: "CHANGE_ME_IN_PRODUCTION"
  
  # Additional secrets can be added here
  # jwt.signing.key: "CHANGE_ME_IN_PRODUCTION"